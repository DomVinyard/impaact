mutation updateImpact(
  $id: uuid!
  $indicator: String!
  $value: numeric!
  $sdg: String!
) {
  update_impact_by_pk(
    _set: { indicator: $indicator, value: $value, sdg: $sdg }
    pk_columns: { id: $id }
  ) {
    id
    indicator
    value
    sdg
  }
}
