mutation insertOrg($author_id: uuid!, $name: String) {
  insert_orgs_one(object: { author_id: $author_id, name: $name }) {
    id
    created_at
    name
    author {
      id
      name
      image
    }
  }
}
