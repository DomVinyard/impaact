query searchOrgs($q: String) {
  orgs(where: { name: { _ilike: $q } }) {
    id
    name
    slug
    description
    main_image
    geography
    impacts(order_by: { priority: asc }, limit: 1) {
      id
      value
      indicator
      sdg
    }
  }
}
