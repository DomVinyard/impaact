query searchOrgs($q: String) {
  orgs(where: { name: { _ilike: $q } }) {
    id
    name
    slug
    description
    main_image
    geography
    impacts(order_by: { priority: asc }) {
      id
      value
      indicator
      sdg
    }
  }
}
