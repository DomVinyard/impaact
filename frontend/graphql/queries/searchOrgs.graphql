query searchOrgs($q: String) {
  orgs(where: { name: { _ilike: $q } }) {
    id
    name
    slug
    description
    main_image
    geography
    impacts {
      id
      value
      indicator
      sdg
    }
  }
}
