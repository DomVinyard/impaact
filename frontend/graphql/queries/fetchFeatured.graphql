query fetchFeatured($top: Int) {
  orgs(order_by: { name: asc }, limit: $top) {
    id
    created_at
    name
    slug
    description
    main_image
    geography
    impacts {
      id
      value
      indicator
      sdg
    }
  }
}
