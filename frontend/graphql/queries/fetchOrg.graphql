query fetchOrg($slug: String) {
  orgs(where: { slug: { _eq: $slug } }) {
    id
    created_at
    name
    author_id
    slug
    description
    main_image
    geography
    impacts(order_by: { priority: asc }) {
      id
      value
      indicator
      sdg
    }
  }
}
