query fetchLatest($top: Int) {
  orgs(order_by: { created_at: desc }, limit: $top) {
    id
    created_at
    name
    slug
    description
    main_image
    geography
    impacts {
      id
      value
      indicator
      sdg
    }
  }
}
